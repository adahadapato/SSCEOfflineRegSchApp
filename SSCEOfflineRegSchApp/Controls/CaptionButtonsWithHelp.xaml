<UserControl x:Class="SSCEOfflineRegSchApp.CaptionButtonsWithHelp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SSCEOfflineRegSchApp"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="{Binding Path=CaptionButtonMargin}">
        <local:HelpButton Cursor="Hand" ToolTip="Get Help"/>
        <local:MinimizeButton Cursor="Hand" ToolTip="Minimize window" CornerRadius="0"/>
        <local:MaximizeButton Cursor="Hand" ToolTip="Restore window"/>
        <local:CloseButton Cursor="Hand" ToolTip="Exit Application"/>
    </StackPanel>
</UserControl>
